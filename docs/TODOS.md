# TODOS

## P1 — Required before launch

| Item | Why | Context | Effort | Depends on |
|---|---|---|---|---|
| Progressive CV disclosure latency validation | The streaming infrastructure (server-sent events / WebSocket) only pays off if per-tool sub-task P50 latency exceeds ~1.5s. Measure before committing to the streaming path. Also confirm CV architecture can emit results per-tool (not batched). | Added in CEO review 2026-04-26 | S | CV pipeline built |
| Skeptic eval set — build and baseline | A fixed set of architecture descriptions with known failure modes and expected caveat tiers. Runs on every Skeptic prompt change. Without this, quality regression after prompt changes is invisible until users report it. | Added in CEO review 2026-04-26 | M | Skeptic agent built |
| Run Pack pricing validation | Run Pack is set at $9 / 5 runs ($1.80/run). Validate that actual per-run cost (LLM tokens + search) is below $1.80 with typical cache hit rates. Adjust price at launch if cost data says otherwise. | Added in CEO review 2026-04-26 | S | Production runs measured |
| Cross-account access prohibition | Read-time enforcement that every tenant-scoped query filters on `runs.tenant_id` (or equivalent). Without this, any user with a known run UUID could access another tenant's data. Schema lock for `runs.tenant_id` lands in Phase 3.4.6; behavior PR is unscoped. | Added 2026-05-08 | M | Phase 3.4.6 schema lock; Phase 4f real auth |
| Account-to-tenant binding immutability | Once a `users.tenant_id` is set, it must not change (DB constraint or app-layer guard). Prevents tenant takeover via re-assignment and ensures `runs.tenant_id` denormalization stays consistent. | Added 2026-05-08 | S | Phase 3.4.6 schema lock |
| Auth provider routing dispatcher (Phase 4f addendum) | Phase 4f as written covers MFA + email verification + IP rate limiting but does NOT explicitly call out reading `tenants.auth_provider` to dispatch login to Clerk vs WorkOS. Without this, the schema we just locked has no consumer. Either fold into 4f scope or split as Phase 4f.1. | Added 2026-05-08 | M | Phase 3.4.6 schema lock; Phase 4f real auth |
| Free-tier BYOK gate (lifetime trial + daily cap) | Enforce: 1 system-paid trial run per user (lifetime), then all free-tier runs require BYOK, 3-per-day cap, then per-run purchase. Schema needs a way to track "lifetime trial used" — either a `users.lifetime_trial_used_at` column or query `runs` for the user's first system-keyed run. Behavior: at run submission, gate on (a) has trial been used? (b) is BYOK registered? (c) daily count under cap? BYOK registration UI surfaces first-class free-key signup links for each registered provider, friction reduction at the lifetime-trial-then-BYOK transition. Provider registry now has Anthropic, DeepSeek, Gemini, Kimi, Mistral, OpenAI, Together (added 2026-05-08). Spec landed 2026-05-08; implementation queued. Run Pack future is open — BYOK serves the iteration use case for free, so multi-pack rationale is shaky. | Added 2026-05-08 | M | Phase 3.5a.1 + 3.5a.1.b complete (BYOK schema in place); decision on Run Pack future |

## P3 — Post-traction

| Item | Why | Context | Effort | Depends on |
|---|---|---|---|---|
| API access tier | Enterprise teams want to embed recommendations into CI/CD, internal onboarding tools, and standards enforcement. Design the API input/output contract after seeing what paying users actually want to extract — not before. | Added in CEO review 2026-04-26 | L | Web product has traction; structured output schema stable |
| Seasonal and novelty theme UI | Enable admin to activate time-bounded themed experiences (pride mode, Christmas, rave mode) and expose a user-level opt-in alongside the light/dark toggle. Database schema is fully in place: custom_css on themes, valid_from/valid_until on theme_assignments, user_theme_preferences stub table. UI and resolution logic deferred. | Added 2026-04-30 | M | Admin themes panel built (Phase 5) |
| Marketing video strategy | Two-video structure: (1) Treehouse-style animated explainer showing the system analyzing a codebase, producing exec-summary consolidation plan, then magic-cutting to detailed spec+plan with zoom-ins on actual artifact content (a phase from a real plan, settled-decision callout, dependency graph), closing with animations of engineering teams executing on the spec and plan (positions the system as a multiplier, not a replacement). (2) Live-action voice-over comparison of agent12 cost and timeline vs typical consultancy migration ($100k-500k, 6-18 months, PowerPoint deliverables). The contrast makes the price point feel trivial. Engineer-persona watches first video, sends second video to their manager along with a Pass 1 output. Show actual artifact content in zoom-ins; abstract animations don't convert. | Added 2026-05-07 | M | First production runs available to record |
| LLM-based PR redteam automation | GitHub Action runs Claude on each PR diff with anti-doc framing. Catches subtle field-level integration drift that types and knip miss. ~$1/PR with caching. | Added 2026-05-07 | M | Phase 4 complete; meaningful E2E surface |
| Knip dead code detection | Configure knip (unused files / exports / deps) and add to CI. Deferred from Phase 3.4 because most current findings are Phase 4 entry-point scaffolding (false positives), and the audit-class bug it was sold to catch (tested-but-unwired) actually slips past knip. Revisit when Phase 4 lands and the scaffolding starts being consumed. | Added 2026-05-07 | S | Phase 4 frontend complete |
| Mermaid validator unit test | The `isValidMermaidSyntax` helper in technical-writer.eval.ts is a pure function (no LLM call) but only exercised inside the LLM eval today. Move to a non-eval unit test so a regression in the validator surfaces without burning tokens. Cleanup, not token savings. | Added 2026-05-07 | S | None |
| User offboarding policy | When a user account is deleted or suspended, we keep their runs (audit trail) but block access. Suspend (not delete) the user record; revoke `user_secrets`; provider-side webhook on user.deleted updates our state. Schema in place; policy + flow undesigned. | Added 2026-05-08 | M | Phase 4f real auth |
| IP allowlisting as tenant config | Enterprise tenants may require login attempts only from approved IP ranges. Stored as tenant config (allowlist of CIDR blocks). Enforced at session-creation time. | Added 2026-05-08 | S | Phase 4f real auth; tenant config UI |
| Shareable links respect tenant boundaries | If shareable run links ship, the link target's tenant must match the viewer's tenant (or the run must be explicitly marked public). Ties to cross-account access prohibition; needs link-shape decision (signed URL vs DB token). | Added 2026-05-08 | S | Decision on shareable link feature; Phase 4f |
| Auth provider webhook idempotency | When Clerk or WorkOS webhooks land (user.created, user.updated, organization.updated), we need an idempotency table so retries do not double-process. Pattern: `auth_webhook_events` with provider, event_id (unique), processed_at, error. Optional at MVP if we lazily look up on demand instead. | Added 2026-05-08 | S | Phase 4f real auth; first webhook consumer |
| Code-aware cost transparency: drop static pricing page? | Code-aware is gated by signup + BYOK + MCP setup, so the pre-auth pricing page audience is effectively zero (anyone serious enough to use code-aware is signing up and has an LLM account already). Consider dropping the static page and relying solely on `estimate_digest_cost` + a "get estimated token cost" link in BYOK setup UI. Simpler surface, single source of truth. Update the BYOK cost transparency settled-decision row and the Cost transparency spec body subsection together. | Added 2026-05-11 | S | None |
| Pre-expiry warning email for code-aware digest drafts | At MVP, expired drafts are deleted with no advance notice. Active-user-at-hard-cap is a real edge case: a user actively engaged at day 89 gets deleted at day 90 with no warning, forced to re-submit (paying another BYOK eval round for work they basically already did). Ship a warning email at ~day 80 (configurable via admin dashboard, per-tenant) once user feedback confirms the issue bites. Strongest case is at the hard cap, not the sliding TTL. | Added 2026-05-14 | S | Code-aware MVP shipped; user feedback validates |
